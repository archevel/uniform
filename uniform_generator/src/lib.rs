/*
use std::collections::HashMap;
use uniform_model::{Entity};

pub struct Template {
    overview: &str;
    view: &str;
}

pub trait Generator {
    fn generateAppSetup() -> void;
    fn generateEntityHandler(&self, localized_templates: HashMap<&str,Template>, entity: Entity) -> void;
    fn build() -> void;
}


*/